export interface IOrder {
    id: number,
    name: string,
    items: number[],
    priceTotal: number
}

export interface IProduct {
    id: number,
    name: string,
    price: number
}

// crea un array de diez ordenes y diez productos


